# PostgreSQL (Docker)
POSTGRES_USER=pcp
POSTGRES_PASSWORD=altere-senha-forte-do-banco
POSTGRES_DB=pcp_homol
POSTGRES_PORT=5432

# API NestJS
BACKEND_PORT=3000
DATABASE_URL=postgresql://pcp:altere-senha-forte-do-banco@localhost:5432/pcp_homol?schema=public
JWT_SECRET=gere-um-segredo-longo-e-aleatorio
JWT_EXPIRES_IN=8h
JWT_EXPIRES_SECONDS=28800
CORS_ORIGIN=http://localhost:5175

# Login — único usuário admin
ADMIN_USERNAME=admin
# Gere o hash: node -e "require('bcrypt').hash('SUA_SENHA',12).then(console.log)"
ADMIN_PASSWORD_HASH=
# Alternativa (só homologação): senha em texto. Em produção use ADMIN_PASSWORD_HASH.
# ADMIN_PASSWORD=
